<div class="blocks">
	<% if schedule_layout_values["dep_country_code"] == @country &&    schedule_layout_values["arr_country_code"] == @country   %>
		<h2><%= I18n.t('flight_schedule.top_dom_airlines_from')%> <%= dep_city_name%></h2>
	<% else %>
		<h2><%= I18n.t('flight_schedule.top_int_airlines_from')%> <%= dep_city_name%></h2>
	<% end %>
	<ul class="list-unstyled top-airlines">
		<% top_airlines.each do |cc| %>
			<li>
					<!-- <div class="col-xs-3 no-padding"> -->
						<!-- <img src="<%#= assets_path%>/new_images/airlines/<%=cc%>.png" alt="<%#= t("airlines.#{cc}") %>"> -->
						<%#formated_carrier_name =  t("airlines.#{cc}").downcase.gsub(" ",'-').gsub('--','-') %>
							<!-- <span class="carrier-logo <%#= formated_carrier_name %>"> -->
		                 	<!-- </span> -->
		 			 <!-- </div>   -->
				<div class="<%= schedule_layout_values["country_code"]=='IN' && (schedule_layout_values["dep_country_code"]=='IN' && schedule_layout_values["arr_country_code"]=='IN') ? 'col-xs-5 text-center' : 'col-xs-8' %> pad-5">
					<% airline_name = link_to( t("airlines.#{cc}"), "#{use_helper.host_name(schedule_layout_values["country_code"])}/flight-booking/" + format_airline_name(I18n.with_locale(:en) { t("airlines.#{cc}") }).downcase.gsub(" ", "-").gsub("--","-") + ".html") %>
					<%= airline_name %>
				</div>
				<div class="col-xs-4 pad-rzero">
					<%= link_to( "Book", "#{use_helper.host_name(schedule_layout_values["country_code"])}/flight-booking/" + t("airlines.#{cc}").downcase.gsub(" ", "-").gsub("--","-") + "-"+ url_escape("#{dep_city_name}-#{arr_city_name}-flights") + ".html",:class=>"booking") %>
				</div>
				<div class="clearfix"></div>
			</li>
		<% end %>

	</ul>
</div>
